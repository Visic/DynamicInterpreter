--Consider adding negative matches (non-consuming, will just make the parser fail if the predicate matches)
--Consider adding inverse matches
--Consider adding range support (e.g. any lowercase char [a-z])
--Add support for the empty string match ''
--Evaluate the performance of the generated parsers and optimize if needed